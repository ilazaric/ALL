https://www.scrapingcourse.com/cloudflare-challenge

^ website is for testing if your script can break through cloudflare

attempt_6.py seems best

```
uc = undetected
xvfb = faking X11, so the browser window doesnt open
ozone-platform=x11 forces browser to use X11 instead of wayland
```
