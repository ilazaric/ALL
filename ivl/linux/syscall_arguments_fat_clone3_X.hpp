// clone3 is too complex, exposing fat_clone wrapper instead
//                        this is to make variables easier to declare ---v---------------------------------
X(4, fat_clone3, const clone_args*, args, size_t, size, void*, fnarg, decltype((void (*)(void*) noexcept)0), fn)
